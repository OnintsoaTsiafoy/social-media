/** Product name shown in the sidebar and page title (kept from the design mock-up). */
export const PRODUCT_NAME = "Pulse";

/** Signed-in admin shown at the bottom of the sidebar until real auth is wired. */
export const CURRENT_ADMIN = { name: "Amine Rahali", initials: "AR", role: "Super Admin" } as const;
